<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        {% block title %}Welcome Team The B.E.S.(T)!{% endblock %}
        
    </head>
    <body>
        {% block body %}
                                
        <a {% if r == 'front' %}active{% endif %}" href="{{ path('accueil')}}">Home</a>
        {% if is_granted('ROLE_USER') %}
        <a {% if r == 'app_logout' %}active{% endif %}" href="{{ path('app_logout')}}"> Déconnexion </a>
        {% else %}
        <a {% if r == 'app_login' %}active{% endif %}" href="{{ path('app_login')}}">  Login </a>
        {% endif %}
           <p> blabla</p>
        {% endblock %}
    </body>
</html>
